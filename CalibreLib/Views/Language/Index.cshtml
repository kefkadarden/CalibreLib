@using CalibreLib.Models.Metadata
@using CalibreLib.Views.Shared.Components
@model Language

@{
    if (Model != null)
    {
       <script>
            language = @Model.Id;
        </script>

        <h2>Language: @Model.LangCode</h2> 
    }
}
@{
    if (Model == null)
    {
        @await Component.InvokeAsync(typeof(ToolbarComponent), new { type = EFilterType.Languages })
    }
}

@{
    if (Model != null)
    {
        <div class="container-fluid">
             @{
                @await Html.PartialAsync("~/Views/Shared/Components/BookCardGrid.cshtml",new List<BookCardModel>())
            }
        </div>
    }
}
