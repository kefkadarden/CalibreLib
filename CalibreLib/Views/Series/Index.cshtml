@using CalibreLib.Models.Metadata
@using CalibreLib.Views.Shared.Components
@model Series

@{
    if (Model != null)
    {
       <script>
            series = @Model.Id;
        </script>

        <h2>Series: @Model.Name</h2> 
    }
}
@{
    if (Model == null)
    {
        @await Component.InvokeAsync(typeof(ToolbarComponent), new { type = EFilterType.Series })
    }
}

@{
    if (Model != null)
    {
        <div class="container-fluid">
             @{
                @await Html.PartialAsync("~/Views/Shared/Components/BookCardGrid.cshtml",new List<BookCardModel>())
            }
        </div>
    }
}
