@using CalibreLib.Models.Metadata
@using CalibreLib.Views.Shared.Components
@model Author

@{
    if (Model != null)
    {
       <script>
            author = @Model.Id;
        </script>

        <h2>Author: @Model.Name</h2> 
    }
}
@{
    if (Model == null)
    {
        @await Component.InvokeAsync(typeof(ToolbarComponent), new { type = EFilterType.Authors })
    }
}

@{
    if (Model != null)
    {
        <div class="container-fluid">
             @{
                @await Html.PartialAsync("~/Views/Shared/Components/BookCardGrid.cshtml",new List<BookCardModel>())
            }
        </div>
    }
}
