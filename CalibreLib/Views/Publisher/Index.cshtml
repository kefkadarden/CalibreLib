@using CalibreLib.Models.Metadata
@using CalibreLib.Views.Shared.Components
@model Publisher

@{
    if (Model != null)
    {
       <script>
            publisher = @Model.Id;
        </script>

        <h2>Publisher: @Model.Name</h2> 
    }
}
@{
    if (Model == null)
    {
        @await Component.InvokeAsync(typeof(ToolbarComponent), new { type = EFilterType.Publishers })
    }
}

@{
    if (Model != null)
    {
        <div class="container-fluid">
             @{
                @await Html.PartialAsync("~/Views/Shared/Components/BookCardGrid.cshtml",new List<BookCardModel>())
            }
        </div>
    }
}
