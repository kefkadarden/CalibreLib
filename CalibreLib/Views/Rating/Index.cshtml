@using CalibreLib.Models.Metadata
@using CalibreLib.Views.Shared.Components
@model Rating

@{
    if (Model != null)
    {
       <script>
            rating = @Model.Id;
        </script>

        <h2>Rating: @Model.Rating1</h2> 
    }
}
@{
    if (Model == null)
    {
        @await Component.InvokeAsync(typeof(ToolbarComponent), new { type = EFilterType.Ratings })
    }
}

@{
    if (Model != null)
    {
        <div class="container-fluid">
             @{
                @await Html.PartialAsync("~/Views/Shared/Components/BookCardGrid.cshtml",new List<BookCardModel>())
            }
        </div>
    }
}
